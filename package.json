{
  "name": "shopify-sort-app",
  "version": "3.0.0",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "build": "cd frontend && npm install && npm run build",
    "migrate": "node src/db/migrate.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "node-cron": "^3.0.3",
    "nodemailer": "^6.9.7",
    "pg": "^8.11.0"
  },
  "engines": { "node": ">=18.0.0" }
}
